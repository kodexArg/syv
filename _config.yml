title: Subordinación y Valor
url: "https://kodexarg.github.io"
baseurl: "/syv"
remote_theme: just-the-docs/just-the-docs
plugins:
  - jekyll-seo-tag
  - jekyll-sitemap

# Default front matter
defaults:
  - scope:
      path: ""
    values:
      layout: default
